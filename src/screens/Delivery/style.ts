import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	container: {
		flex: 1,
		flexGrow: 1,
	},

	content: {
		paddingTop: 16,
		paddingBottom: 16,
	}
});

export default styles;
